# docker-dind-sbt

```bash
# Login to Docker Hub
docker login
# Enter your Docker Hub username and password

# Build the image
docker build -t mabhi256/sbt-dind-awscli:latest .

# Push to Docker Hub
docker push mabhi256/sbt-dind-awscli:latest
```
